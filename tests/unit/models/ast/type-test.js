import { test, moduleForModel } from 'ember-qunit';

moduleForModel('ast/type', 'Ast/Type', {
  // Specify the other units that are required for this test.
  needs: []
});

test('it exists', function() {
  var model = this.subject();
  ok(model);
});
